<div class="container-menu-desktop">
	<!-- Topbar -->
	<div class="top-bar">
		<div class="content-topbar flex-sb-m h-full container">
			<div class="left-top-bar">
				Ecommerce Website - Rookie Testing
			</div>

			<div class="right-top-bar flex-w h-full">

				<a href="#" class="flex-c-m trans-04 p-lr-25">
					VN
				</a>

				<a href="#" class="flex-c-m trans-04 p-lr-25">
					VNĐ
				</a>
				<a href="#" class="flex-c-m trans-04 p-lr-25">
					Contact: +84798222837
				</a>
			</div>

		</div>
	</div>

	<div class="wrap-menu-desktop">
		<nav class="limiter-menu-desktop container">

			<!-- Logo desktop -->
			<a asp-controller="Home" asp-action="Index" class="logo">
				<img src="~/images/icons/logo-01.png" alt="IMG-LOGO">
			</a>
			<!-- Menu desktop -->
			<div class="menu-desktop">
				<ul class="main-menu">
					<li class="active-menu">
						<a asp-controller="Home" asp-action="Index">Home</a>
					</li>

					<li>
						<a asp-controller="Product" asp-action="Index">Shop</a>
					</li>

					<li class="label1" data-label1="hot">
						<a href="#">Features</a>
					</li>

					<li>
						<a href="#">About</a>
					</li>
				</ul>
			</div>

			<!-- Icon header -->
			<div class="wrap-icon-header flex-w flex-r-m">
				<div class="icon-header-item cl2 hov-cl1 trans-04 p-l-22 p-r-11 js-show-modal-search">
					<i class="zmdi zmdi-search"></i>
				</div>

				<div class="icon-header-item cl2 hov-cl1 trans-04 p-l-22 p-r-11 icon-header-noti js-show-cart" data-notify="2">
					<i class="zmdi zmdi-shopping-cart"></i>
				</div>

				<a href="#" class="dis-block icon-header-item cl2 hov-cl1 trans-04 p-l-22 p-r-11 icon-header-noti" data-notify="0">
					<i class="zmdi zmdi-favorite-outline"></i>
				</a>
				<div class="flex-c-m h-full p-lr-19">
					<div class="icon-header-item cl0 hov-cl1 trans-04 p-lr-11 js-show-sidebar">
						<i class="zmdi zmdi-menu"></i>
					</div>
				</div>
			</div>
		</nav>
	</div>
</div>
<script>
	// Hàm để fetch API và cập nhật số lượng mục trong cart
	// function updateCartCount() {
	// 	fetch('localhost:7273/cart/getID/12dec763-64c7-405c-b914-9f9d3d33e5fe')
	// 	.then(response => response.json())
	// 	.then(data => {
	// 		// Lấy số lượng mục từ dữ liệu nhận được từ API
	// 		const cartCount = data.cartItemCount;

	// 		// Cập nhật số lượng trên notify
	// 		document.querySelector('.icon-header-item[data-notify="2"]').setAttribute('data-notify', cartCount);
	// 	})
	// 	.catch(error => {
	// 		console.error('Fetch error:', error);
	// 	});
	// }

	// Hàm để fetch API và cập nhật số lượng mục trong wishlist
	// function updateWishlistCount() {
	// 	fetch('url/api/wishlist')
	// 	.then(response => response.json())
	// 	.then(data => {
	// 		// Lấy số lượng mục từ dữ liệu nhận được từ API
	// 		const wishlistCount = data.wishlistItemCount;

	// 		// Cập nhật số lượng trên notify
	// 		document.querySelector('.icon-header-item[data-notify="0"]').setAttribute('data-notify', wishlistCount);
	// 	})
	// 	.catch(error => {
	// 		console.error('Fetch error:', error);
	// 	});
	// }

	// Gọi hàm cập nhật số lượng mục trong cart và wishlist khi trang web được tải
	document.addEventListener('DOMContentLoaded', function () {
		updateCartCount();
		//updateWishlistCount();
	});

</script>